create table Books (
    id bigint primary key generated always as identity,
    name text,
    price text,
    ratings text,
    availability text
);


CREATE TABLE posts (
  id text PRIMARY KEY,
  text text
);


CREATE TABLE posts (
  id text PRIMARY KEY,
  text text,  
);